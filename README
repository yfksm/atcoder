1. これから参加するコンテスト名をcontest_nameファイルへ入力する。
コンテスト名はatcoderのurlを確認する。

2. 問題を解く。makeコマンドを叩けばコンパイルから全サンプルでのアサートを行える。

3. サンプルケースを全て通ったら、手動で解答をコピペして提出する。

4. make b などで選択している問題を変更する。
/conf/problemを直接編集して選択中の問題を変更しても良い。

※make libuseは実行した分だけlibがソースコードに追加されるので注意